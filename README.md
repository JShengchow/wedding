# wedding

## Deploy

Run a full build, Git push, and Vercel production deployment:

```bash
npm run deploy -- "Update wedding site"
```

The script runs `npm run build`, commits any local changes, pushes the current branch to `origin`, and deploys to Vercel production.
